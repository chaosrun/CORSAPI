name = "corsapi"
main = "_worker.js"
compatibility_date = "2024-01-01"

# Workers 类型配置
workers_dev = true

[vars]
# KV 缓存默认 30 分钟；Cloudflare KV expirationTtl 最小值为 60 秒
CACHE_TTL_SECONDS = "1800"

# KV 命名空间绑定（可选，如果需要使用 KV 缓存请取消注释）
# [[kv_namespaces]]
# binding = "KV"
# id = "your-kv-namespace-id"
